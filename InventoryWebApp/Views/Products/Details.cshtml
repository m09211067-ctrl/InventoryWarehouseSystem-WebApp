@model InventoryWebApp.Models.Product

@{
    ViewData["Title"] = "تفاصيل المنتج";
}

<div class="container mt-4" dir="rtl">
    <h2 class="mb-4 text-center">🔍 تفاصيل المنتج</h2>

    <div class="card shadow-sm">
        <div class="card-body">

            <table class="table table-bordered">
                <tr>
                    <th style="width: 25%;">رقم المنتج</th>
                    <td>@Model.ProductID</td>
                </tr>
                <tr>
                    <th>اسم المنتج</th>
                    <td>@Model.ProductName</td>
                </tr>
                <tr>
                    <th>الكمية</th>
                    <td>@Model.Quantity</td>
                </tr>
                <tr>
                    <th>السعر</th>
                    <td>@Model.Price</td>
                </tr>
                <tr>
                    <th>الباركود</th>
                    <td>@Model.Barcode</td>
                </tr>
                <tr>
                    <th>الوصف</th>
                    <td>@Model.Description</td>
                </tr>
            </table>

            @if (Model.Components != null && Model.Components.Any())
            {
                <h5 class="mt-4">🧩 المكوّنات (للمنتجات المركبة)</h5>
                <ul class="list-group">
                    @foreach (var c in Model.Components)
                    {
                        <li class="list-group-item">@c</li>
                    }
                </ul>
            }

            <div class="mt-3 d-flex justify-content-between">
                <a href="/Products" class="btn btn-secondary">⬅ رجوع للقائمة</a>
                <a href="/Products/Edit/@Model.ProductID" class="btn btn-primary">✏️ تعديل المنتج</a>
            </div>

        </div>
    </div>
</div>
