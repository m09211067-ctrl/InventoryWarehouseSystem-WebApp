@model InventoryWebApp.Models.Product

@{
    ViewData["Title"] = "منتج مركب (Builder Pattern)";
}

<div class="container mt-4" dir="rtl">
    <h2 class="mb-4 text-center">🧩 منتج مركب تم بناؤه باستخدام Builder Pattern</h2>

    <div class="card shadow-sm">
        <div class="card-body">

            <h4 class="mb-3">@Model.ProductName</h4>

            <p><strong>الباركود:</strong> @Model.Barcode</p>
            <p><strong>السعر:</strong> @Model.Price</p>
            <p><strong>الكمية:</strong> @Model.Quantity</p>

            @if (Model.Components != null && Model.Components.Any())
            {
                <h5 class="mt-3">المكوّنات:</h5>
                <ul class="list-group mb-3">
                    @foreach (var c in Model.Components)
                    {
                        <li class="list-group-item">@c</li>
                    }
                </ul>
            }
            else
            {
                <p class="text-muted">لا توجد مكوّنات معرفّة لهذا المنتج.</p>
            }

            <div class="alert alert-info mt-3">
                ✅ هذا المنتج تم إنشاؤه برمجيًا عن طريق
                <strong>ProductDirector</strong> و
                <strong>CompositeProductBuilder</strong>
                (نمط <em>Builder</em>).
            </div>

            <!-- ====== زر الحفظ الفعلي ====== -->
            <form asp-action="SaveComposite" method="post" class="mt-4">

                <div class="mb-3">
                    <label class="form-label">اختر المخزن</label>
                    <select name="warehouseId" class="form-control" required>
                        <!-- مؤقتًا مخزن افتراضي -->
                        <option value="1">المخزن الرئيسي</option>
                    </select>
                </div>

                <button type="submit" class="btn btn-success">
                    💾 حفظ المنتج المركب
                </button>

                <a href="/Products" class="btn btn-secondary ms-2">
                    ⬅ رجوع لقائمة المنتجات
                </a>
            </form>
        </div>
    </div>
</div>
